---
- name: Converge
  hosts: all
  become: true
  vars_files:
    # - ../../defaults/main.yml
    - ../role-stubs/defaults_geerlingguy.jenkins.yml

  roles:
    - role: dankempter.jenkins-config

    - role: ../../../
